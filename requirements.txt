# Agente de Voz Sofía — Gemini Live + LiveKit
# Instalar con: pip install -r requirements.txt

livekit-agents[google]>=1.11.0
livekit-plugins-google>=1.6.0
livekit>=0.19
google-genai>=1.0
python-dotenv>=1.0
httpx>=0.27
